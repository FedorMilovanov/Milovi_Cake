# ✨ r67 — Ювелирная шлифовка мобильной премиум-версии

**Дата:** 2026-06-06 · **Версия:** `?v=20260606r06`, SW `v2026.06.06-r06`
**Принцип:** минимально-инвазивно по `AGENTS.md`. Без новых файлов, без новых эффектов, `!important`-долг НЕ вырос (даже снизился). Только ровность и чистота.

## Что сделано

### Чистка реальных хвостов (то, что прошлые агенты пропустили)
1. **`content-visibility` удалён** из `premium-overrides.css` (риск для SEO-индексации текста и Ctrl+F / скринридеров). Прошлый агент убрал его в `mc-2026.css`, но забыл здесь.
2. **Коричневый литерал `#8a5e23` заменён на брендовый `#b07a3a`** во всех fallback'ах `var(--gold-acc, …)` (mc-2026.css ×3, premium-overrides.css ×9). Теперь даже при сбое переменной кнопки никогда не станут старо-коричневыми. (Оставлен 1 намеренный градиентный якорь deep→gold — он корректен.)
3. **Конфликт `.product-footer` устранён.** Раньше `mc-2026.css` (ряд) и `v20-fixes.css` (колонка) дрались `!important`'ами на одном брейкпоинте 768px — исход решал лишь порядок загрузки («мина»). Теперь единый источник правды — `mc-2026.css`: цена слева / кнопка справа, со сворачиванием в столбик только на ультра-узких ≤360px. Дубль в `v20-fixes.css` нейтрализован.

### Ювелирная шлифовка (ровный, отполированный вид)
4. **Единый спокойный tap-feedback** на всех карточках (товар, feature, отзывы, начинки): одна пружина, один масштаб `scale(0.992)` — убрана «дёрганость».
5. **Ровный ритм радиусов:** карточки 24px, пилюли 50px, чипы 16–18px — единый «комплект».
6. **Спокойные тени:** один мягкий тёплый слой вместо неравномерного «парения».
7. **Кнопка «Заказать»** переведена на фиксированную высоту 46px (вместо плавающего padding) — ровная высота во всех карточках.
8. **Hairline-бордеры 0.5px** на Retina-экранах — чёткие края без «мыла».
9. **`scroll-margin-top`** на секции — якоря не уезжают под шапку.
10. **`prefers-reduced-motion`** уважается — tap-scale отключается для тех, кто просит.

## Чего НЕ трогал (намеренно)
- **Синий ховер букв** — это ВАШ оригинальный дизайн (подтверждено по git: был с первой загрузки). Оставлен как есть.
- Защищённые зоны: hero-мессенджеры, корзина `buildCartKey`, theme-toggle SVG, lightbox пригородов, иконка телефона.
- День/ночь: root-level плавный переход остался нетронутым (работает корректно).

## Проверки (все зелёные)
- `python3 audit.py` → **PASSED, 0 ошибок, 0 предупреждений**
- `node --check` на всех 6 JS-файлах → OK
- `python3 prigorody/build.py` → 14 городов перегенерированы, 0 ошибок
- Версия `?v=` + `CACHE_NAME` синхронны во всех HTML/JS/SW (r06)
- `!important`: v20-fixes 75→59 (−16), mc-2026 = 199 (в бюджете), суммарно не вырос
