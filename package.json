{
  "name": "milovi-cake",
  "private": true,
  "version": "1.0.0",
  "description": "Milovi Cake — Торты на заказ. Professional audit tooling, zero runtime deps.",
  "scripts": {
    "audit": "python3 scripts/audit.py",
    "audit:analytics": "python3 scripts/analytics_contract.py",
    "audit:security": "npm audit --audit-level=high",
    "audit:js": "node --check js/main.js && node --check js/nav.js && node --check js/mc-2026.js && node --check js/consent-analytics.js && node --check js/v20-faq-fix.js && node --check js/gallery/main.js && node --check js/gallery/data.js && node --check sw.js && node --check scripts/css_coverage.cjs && echo '✅ All JS valid'",
    "audit:prigorody": "python3 scripts/check_prigorody_idempotent.py",
    "audit:ip-copy": "python3 scripts/ip_copy_contract.py",
    "audit:video-schema": "python3 scripts/video_schema_contract.py",
    "audit:lighthouse-aggregation": "python3 scripts/lighthouse_aggregation_contract.py",
    "audit:all": "npm run audit:js && npm run audit:analytics && npm run audit:a11y && npm run audit:release && npm run audit:lighthouse-regressions && npm run audit:lighthouse-aggregation && npm run audit:ip-copy && npm run audit:video-schema && npm run audit:prigorody && npm run audit:security && npm run audit",
    "build:prigorody": "python3 prigorody/build.py",
    "build:pages": "python3 scripts/build_pages_artifact.py",
    "audit:pages": "python3 scripts/audit_pages_artifact.py",
    "test:playwright": "playwright test",
    "test:playwright:headed": "playwright test --headed",
    "test:playwright:report": "playwright show-report",
    "qa": "npm run audit:js && npm run audit:analytics && npm run audit:a11y && npm run audit:release && npm run audit:lighthouse-regressions && npm run audit:lighthouse-aggregation && npm run audit:ip-copy && npm run audit:video-schema && python3 scripts/check_prigorody_idempotent.py && npm run audit:security && npm run audit && npm run test:playwright",
    "smoke:prod": "python3 scripts/production_smoke.py",
    "indexnow:dry-run": "python3 scripts/submit_indexnow.py --dry-run",
    "indexnow:submit": "python3 scripts/submit_indexnow.py",
    "smoke:prod:retry": "python3 scripts/production_smoke.py --retries 6 --delay 30",
    "coverage:css": "node scripts/css_coverage.cjs",
    "optimize:image": "python3 scripts/optimize_image.py",
    "visual:snapshot": "node scripts/visual_snapshot.cjs",
    "visual:diff": "python3 scripts/visual_diff.py",
    "audit:a11y": "python3 scripts/a11y_static.py",
    "audit:release": "python3 scripts/release_contract.py",
    "smoke:prod:release": "python3 scripts/production_release_smoke.py",
    "audit:lighthouse-regressions": "python3 scripts/lighthouse_regressions.py"
  },
  "engines": {
    "node": ">=22.13.0",
    "npm": ">=10.9.2"
  },
  "keywords": [
    "cakes",
    "milovi",
    "static-site"
  ],
  "license": "UNLICENSED",
  "devDependencies": {
    "@playwright/test": "1.63.0",
    "playwright": "1.63.0"
  }
}
