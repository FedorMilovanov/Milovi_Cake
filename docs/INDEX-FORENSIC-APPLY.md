# Apply audited INDEX fixes

This marker requests the eleventh deterministic forensic pass: remove duplicated footer assertions from the test harness and verify that the hidden mobile back-to-top control restores correctly after leaving the footer.

It has no production runtime effect and will be removed after verification.
