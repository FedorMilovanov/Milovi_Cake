# Apply audited INDEX fixes

This marker requests the twelfth deterministic forensic pass: keep review copy readable during its outgoing transition and prevent incidental browser alignment scroll from immediately closing the mobile calculator result panel.

It has no production runtime effect and will be removed after verification.
