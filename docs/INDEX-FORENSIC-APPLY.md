# Apply audited INDEX fixes

This marker requests the deterministic fixes recorded by the forensic audit. The product fix already targets the real `#fillings` section; this pass only removes the duplicated footer assertion in the test harness.

It has no production runtime effect and will be removed after verification.
