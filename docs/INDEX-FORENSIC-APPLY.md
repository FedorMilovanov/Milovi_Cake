# Apply audited INDEX fixes

This marker requests the deterministic fixes recorded by the forensic audit. The fourth pass adds the confirmed mobile calculator stacking correction and re-runs footer geometry only at the real document end.

It has no production runtime effect and will be removed after verification.
