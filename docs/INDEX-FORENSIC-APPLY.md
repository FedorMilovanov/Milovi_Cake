# Apply audited INDEX fixes

This marker requests the deterministic fixes recorded by the forensic audit. The final correction targets the real `#fillings` calculator section rather than a nonexistent `#calculator` node.

It has no production runtime effect and will be removed after verification.
