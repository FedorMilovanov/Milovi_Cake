# INDEX forensic audit trigger

Temporary exact-head trigger for the full desktop/mobile, light/dark, interaction and external validation pass on 2026-08-03.

Clean control verification runs on the exact head after the stacking context was moved from the nonexistent `#calculator` selector to the real `#fillings` section and the duplicated audit assertion was removed.

This user-authored trigger intentionally restarts the PR workflows after the bot-authored correction commits.

No production UI or runtime behavior is changed by this file.
